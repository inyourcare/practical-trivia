export interface PostMetadata {
  // title: string;
  // date: string;
  // subtitle: string;
  // slug: string;
  id: string;
  tags: Array<string>;
  krTags: Array<string>;
  category: Array<string>;
  date: string;
  title: string;
  description: string;
  image: string;
  slug: string;
}
